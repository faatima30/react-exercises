const Header = () => <header>Header</header>;
const Footer = () => <footer>Footer</footer>;

const Layout = () => (
  <div>
    <Header />
    <main>Content</main>
    <Footer />
  </div>
);
export default Layout;
